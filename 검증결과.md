# 새 AWS 배포 프로젝트 검증 기록

검증일: 2026-09-18. 대상: `chatbot_aws`와 연결된 3일차 가이드.

## 1. 반영한 구성

- React는 비공개 프론트 S3·CloudFront에서 제공하고 Django API만 ECR·EC2에서 실행.
- RDS MySQL은 비공개로 연결하며 CA와 서버 이름을 검증하는 TLS 사용.
- 기존 비공개 S3에는 로그인한 사용자의 대화 TXT 저장. 다운로드 URL은 10분 유효.
- 로컬 수동 배포 후 GitHub Actions OIDC·SSM으로 프론트와 백엔드를 자동 배포.
- 사용자 도메인·ACM·ALB는 마지막 확장 단계. 원본용 Host와 브라우저 Origin 설정을 구분.

## 2. 실행하여 확인한 항목

| 항목 | 결과 | 검증 범위 |
| --- | --- | --- |
| Django 테스트 | 30개 통과 | 회원·대화·S3 권한/오류 처리, 배포 설정, CloudFront 방식 Host/Origin과 CSRF |
| 프론트 API 테스트 | 6개 통과 | API 요청·응답 처리 |
| 프론트 Vite 빌드 | 통과 | React 배포 파일 생성 |
| 배포 도구 테스트 | 5개 통과 | 입력 검증·SSM/공개 응답 모의 검증 |
| Compose 구성 검사 | 로컬·운영 통과 | 안전한 예제 값으로 config --quiet 확인 |
| 정적 검사 | 통과 | Python 문법, Bash 문법, Compose·Actions YAML, IAM·OAC JSON |
| 노션 반영 | 재조회 확인 | 3일차 순서·index·본문·페이지 이동 링크, 2일차 연결 설명 |

Django 테스트는 별도 Python 환경과 SQLite 테스트 설정에서 실행했으며 S3/AWS 호출은 모의 처리함. CloudFront 테스트는 실제 AWS 트래픽이 아닌 Django 테스트 클라이언트로 Host·Origin·HTTPS 전달 상황을 재현한 결과임.

## 3. 아직 실행하지 않은 항목

이 작업에서 EC2·RDS·S3·ECR·CloudFront·ACM·ALB를 생성하거나 변경하지 않음. 실제 AWS 배포, RDS TLS 접속, IAM/OIDC·SSM 권한, S3 업로드와 다운로드, GitHub Actions 실행 성공은 학원 계정에서 가이드 순서대로 확인해야 함.

현재 PC의 Docker 데몬이 실행되지 않아 새 Docker 이미지 빌드와 Compose 전체 컨테이너 실행은 수행하지 못함. Compose 구성 검사와 로컬 단위 테스트 통과를 실제 배포 완료로 해석하지 않음.

## 4. 실제 배포에서 확인할 결과

1. CloudFront HTTPS 주소의 `/api/health/`가 `status=ok`, `database=ok` 반환.
2. 같은 CloudFront 주소에서 회원가입·로그인·채팅·새로고침 후 대화 유지.
3. 대화 TXT 내보내기 → 기존 S3 버킷에 객체 생성 → 임시 URL 다운로드.
4. GitHub main 변경 → 새 ECR 태그 → SSM 배포 성공 → 프론트 화면 변경과 기존 대화 유지.
5. 사용자 도메인 HTTPS → CloudFront → HTTPS ALB → EC2 연결.
6. 새 환경의 위 결과가 확인된 후 기존 EC2 정리.

배포 실패는 자동 복구하지 않음. 실패 직후 마지막 성공 이미지로 복구할 때는 `deploy/release.env`, 성공한 배포를 이전 성공 버전으로 되돌릴 때는 `deploy/previous.env`를 사용. DB 스키마·데이터는 이미지 복구와 별도로 검토.

## 5. 원본과 배포 파일

기존 `13_docker/_chatbot_fullstack`과 이전 `day3_kit`은 이번 프로젝트로 덮어쓰지 않음. 새 프로젝트의 `.env.example`은 예시값으로 작성. 실제 `.env`, 개인 키·인증서, DB 파일, 가상환경, node_modules, 빌드 결과, Git 기록은 배포 압축에 포함하지 않음.

